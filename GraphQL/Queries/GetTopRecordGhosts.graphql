query GetTopRecordGhosts($xxHash: String!, $hash: String!, $first: Int = null) {
  records(
    filter: {
      recordMediaExists: true
      level: {
        or: [
          { xxHash: { equalTo: $xxHash } }
          {
            and: [
              { hash: { equalTo: $hash } }
              { adventure: { equalTo: true } }
            ]
          }
        ]
      }
    }
    first: $first
    orderBy: TIME_ASC
  ) {
    nodes {
      ...GhostRecordFrag
    }
  }
}