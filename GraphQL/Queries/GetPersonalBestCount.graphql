query GetPersonalBestCount($xxHash: String!, $hash: String!) {
    personalBestGlobals(
        filter: {
            level: {
                or: [
                    { xxHash: { equalTo: $xxHash } }
                    {
                        and: [
                            { hash: { equalTo: $hash } }
                            { adventure: { equalTo: true } }
                        ]
                    }
                ]
            }
        }
    ) {
        totalCount
    }
}
